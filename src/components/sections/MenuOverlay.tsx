// TODO (Phase 5): full-screen overlay with nav links and the 3 feature cards.
// Phase 6.2: clip-path reveal, staggered links, lenis.stop() while open.
export function MenuOverlay() {
  return null;
}
