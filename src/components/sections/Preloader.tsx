// TODO (Phase 6.1): landscape SVG, tagline, 00→100 counter, reveal transition.
// Stops Lenis until fonts + hero images load (4s max), then ScrollTrigger.refresh().
export function Preloader() {
  return null;
}
