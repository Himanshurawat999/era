// TODO (Phase 8.1): form wired to `bookCall` in app/actions.ts via useActionState,
// with idle / submitting / success / error states and a hidden `company` honeypot.
export function BookCallForm() {
  return null;
}
